node_modules/
dist/
*.local
.DS_Store

# generated model masters (committed on purpose — regenerated by `npm run models`)
# NOTE: public/models/*.glb + *.json ARE tracked; only ignore the raw intermediate.
