# docs/diagrams

> **Historical.** `architecture.svg` shows the **v0.2 two-controller BLE split**
> (LEFT peripheral ⇄ BLE ⇄ RIGHT central) — an architecture abandoned in v0.3.
> The current rev-A design is a **single** E73 (nRF52840) in the right grip
> scanning one 9×10 matrix, with the passive left grip wired over a 16-way FFC
> jumper. See the architecture diagram in the [README](../../README.md#how-it-works--matrix-gpio-power-antenna)
> and [connectivity-and-power.md](../connectivity-and-power.md).

- `architecture.svg` — v0.2 split architecture (historical, kept for the record).
