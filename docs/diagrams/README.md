# docs/diagrams

- `architecture.svg` — system architecture: LEFT (peripheral) ⇄BLE⇄ RIGHT
  (central) ⇄ host (BLE or USB-C). See `../connectivity-and-power.md`.

`TODO(user)`: add a mechanical/clamp interface diagram once the 3D shell is
designed against the 63 × 108 mm per-half envelope.
